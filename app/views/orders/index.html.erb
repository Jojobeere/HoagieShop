<h1>Orders</h1>
<table>
  <tr>
    <th>Id</th>
    <th>Customer</th>
    <th>Status</th>
    <th>Price</th>
  </tr>
  <% @orders.each do |order| %>
    <tr>
      <td><%= order.id %></td>
      <td><%= order.customer_id %></td>
      <td><%= order.status %></td>
      <td><%= order.price %></td>
    </tr>
  <% end %>
</table>
